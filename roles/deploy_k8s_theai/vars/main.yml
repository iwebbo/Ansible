# vars/main.yml
project_name: "theai"

release_version: ""   # passé par pipeline (ex: v1.0)

# MinIO - To be set before to deploy via minio-client job
# name & config dans ~/.mc/config.json
minio_alias: "production"
minio_bucket: "theai"

# Local paths
work_dir: "/tmp/{{ project_name }}-release-{{ release_version }}"
backend_tar: "{{ work_dir }}/{{ project_name }}-backend-{{ release_version }}.tar"
frontend_tar: "{{ work_dir }}/{{ project_name }}-frontend-{{ release_version }}.tar"
